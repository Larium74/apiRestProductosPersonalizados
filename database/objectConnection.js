import { HOST_DB, DB_NAME, USER_DB, PASSWORD_DB, PORT_DB} from "../config.js"
export let objectConnection = {
    host: HOST_DB,
    database: DB_NAME,
    user: USER_DB,
    password: PASSWORD_DB,
    port: PORT_DB
}